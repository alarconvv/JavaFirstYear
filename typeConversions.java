// ty convertions 
// it is for generate temporall entities of a new type 

public class typeConversions
{
    public static void main (String [ ] args)
    {
        double quotient;
        int x = 6;
        int y = 10;
        quotient = (double) x / y;
        System.out.println(quotient);
    }
}